'use client';

import { useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { Loader2, Palette, Plus, RefreshCw } from 'lucide-react';
import { useRouter } from 'next/navigation';
import { motion } from 'framer-motion';
import api from '@/lib/api';
import { Modal } from './Modal';
import { CoSpaceContext } from './CoSpaceContext';

type Canvas = { id: string; title: string; updatedAt: string };
export function CoCanvasList({ workspaceId }: { workspaceId: string }) {
  const router = useRouter(); const queryClient = useQueryClient();
  const [showCreate, setShowCreate] = useState(false); const [title, setTitle] = useState(''); const [error, setError] = useState('');
  const { data: canvases = [], isLoading, isError, refetch } = useQuery({ queryKey: ['co-canvases', workspaceId], queryFn: async () => (await api.get(`/workspaces/${workspaceId}/canvases`)).data.data as Canvas[] });
  const create = useMutation({ mutationFn: async () => (await api.post(`/workspaces/${workspaceId}/canvases`, { title: title.trim() })).data.data as Canvas, onSuccess: (canvas) => { queryClient.invalidateQueries({ queryKey: ['co-canvases', workspaceId] }); setShowCreate(false); setTitle(''); router.push(`/co-space/${workspaceId}/canvas/${canvas.id}`); }, onError: () => setError('Could not create the canvas. Try again.') });
  return <div className="mx-auto max-w-6xl px-4 py-8 sm:px-6 md:px-10"><CoSpaceContext workspaceId={workspaceId} current="canvas" /><header className="mb-9 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-accent-blue">Shared drawing space</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">Co-Canvas</h1><p className="mt-3 max-w-lg text-sm text-muted">Build maps, plans, and notes together in a shared board.</p></div><button onClick={() => { setError(''); setShowCreate(true); }} className="inline-flex h-10 items-center justify-center gap-2 rounded-xl bg-accent-blue px-4 py-2 text-sm font-semibold text-white shadow-xs hover:bg-accent-blue-hover cursor-pointer"><Plus size={17} />New canvas</button></header>{isLoading ? <div className="py-20 text-center text-muted"><Loader2 className="mx-auto animate-spin" /><p className="mt-2 text-sm">Loading shared canvases...</p></div> : isError ? <div role="alert" className="rounded-2xl border border-dashed border-border bg-surface p-10 text-center"><Palette className="mx-auto text-muted" /><h2 className="mt-4 font-semibold">Canvases are unavailable</h2><p className="mt-2 text-sm text-muted">We couldn’t load this shared space.</p><button onClick={() => refetch()} className="mt-5 inline-flex h-10 items-center gap-2 rounded-xl border border-border px-4 text-sm font-semibold hover:bg-surface-hover cursor-pointer"><RefreshCw size={15} />Try again</button></div> : canvases.length ? <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">{canvases.map((canvas) => <motion.button whileTap={{ scale: 0.99 }} key={canvas.id} onClick={() => router.push(`/co-space/${workspaceId}/canvas/${canvas.id}`)} className="min-h-44 rounded-2xl border border-border bg-surface p-5 text-left hover:bg-surface-hover cursor-pointer"><Palette size={22} className="text-accent-blue" /><h2 className="mt-10 truncate text-lg font-semibold">{canvas.title}</h2><p className="mt-2 text-xs text-muted">Updated {new Date(canvas.updatedAt).toLocaleDateString()}</p></motion.button>)}</div> : <div className="rounded-2xl border border-dashed border-border p-10 text-center"><Palette className="mx-auto text-muted" /><h2 className="mt-4 font-semibold">Start the first shared canvas</h2><p className="mt-2 text-sm text-muted">Everyone in this Co-Space can see it and contribute.</p><button onClick={() => setShowCreate(true)} className="mt-5 inline-flex h-10 items-center gap-2 rounded-xl bg-accent-blue px-4 text-sm font-semibold text-white hover:bg-accent-blue-hover cursor-pointer"><Plus size={15} />Create canvas</button></div>}<Modal isOpen={showCreate} onClose={() => !create.isPending && setShowCreate(false)} title="Create shared canvas"><form onSubmit={(event) => { event.preventDefault(); if (title.trim()) create.mutate(); }} className="space-y-4"><div><label htmlFor="canvas-title" className="mb-2 block text-sm font-medium">Canvas name</label><input id="canvas-title" autoFocus value={title} onChange={(event) => setTitle(event.target.value)} placeholder="e.g. Launch plan" className="w-full rounded-xl border border-border bg-background px-3 py-2.5 text-sm outline-none focus:border-accent-blue" /></div>{error && <p role="alert" className="rounded-lg bg-red-500/10 px-3 py-2 text-sm text-red-600">{error}</p>}<button disabled={create.isPending || !title.trim()} className="inline-flex h-11 w-full items-center justify-center gap-2 rounded-xl bg-accent-blue px-4 text-sm font-semibold text-white disabled:opacity-50 cursor-pointer">{create.isPending && <Loader2 size={16} className="animate-spin" />}Create canvas</button></form></Modal></div>;
}
