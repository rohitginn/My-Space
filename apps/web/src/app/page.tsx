import { LandingPage } from '@/components/LandingPage';

export const metadata = {
  title: 'Personal productivity and collaboration workspace',
  description: 'MySpace brings planning, tasks, projects, notes, habits, focus sessions, and collaborative Co-Spaces into one calm workspace.',
};

export default function Home() {
  return <LandingPage />;
}
